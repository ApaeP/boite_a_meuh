<nav>
<!-- navbar top -->
  <div class="navbar-top">
    <div class="navbar-logo">
      <div class="home"> <%= link_to image_tag("logo_moobox_light.png"), boxes_path %> </div>
    </div>

    <!-- Theme switch -->
    <div class="theme-items">
      <%= link_to "", root_path(theme: "#{ current_opposite_theme }"), id: "theme-link" %>
      <i class="<%= theme_is_dark? ? 'fas' : 'far' %> fa-moon"></i>
        <% if cookies[:theme] == "dark" %>
          <input id="theme-toggle" type="checkbox" class="toggle-theme" checked>
        <% else %>
          <input id="theme-toggle" type="checkbox" class="toggle-theme">
        <% end %>
      <i class="<%= theme_is_dark? ? 'far' : 'fas' %> fa-moon"></i>
    </div>
    <!-- Theme switch end -->
  </div>

<!-- navbar bottom -->
  <div class="navbar-bottom">
    <!-- navbar buttons -->
    <ul>
      <!-- a quoi servent les classes des div dans les li? -->
      <!-- pourquoi des div dans des li? -->
      <!-- 😃 -->
      <li> <div class="index"> <%= link_to "Mooboxes", boxes_path %> </div> </li>
      <li> <div class="create-moobox"> <%= link_to "Create a Moobox", new_box_path %> </div> </li>
      <li> <div class="sounds"> <%= link_to "Sounds", sounds_path %> </div> </li>
      <li> <div class="create-sounds"> <%= link_to "Create a sound", new_sound_path %> </div> </li>
      <li> <div> <%= link_to "Styles", styles_path %> </div> </li>
      <li> <div> <%= link_to "Create a style", new_style_path %> </div> </li>
    </ul>
  </div>
</nav>
